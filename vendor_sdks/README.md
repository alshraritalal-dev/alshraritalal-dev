# Vendor SDK Drops

Place separately licensed SDKs here using stable folder names:

- `dlss/`
- `fmod/`
- `fsr/`
- `wwise/`
- `xess/`

These SDKs are disabled by default in CMake. Enable them only after the license files, headers, libraries, and runtime redistributables are present locally.
